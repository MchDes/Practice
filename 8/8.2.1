#include <stdio.h>
#define size 100


int main()
{
    arr[100][100];
    int count_row = 0;
    int count_column = 0;
    scanf("%d %d", count_row, count_column);
    for (int i, i < count_row, ++i)
    {
        for(int j, j < count_column, ++j)
        {
            scanf("%d %d", arr[i][j]);
        }
    }
    for (int i, i < count_row, ++i)
    {
        for(int j, j < count_column, ++j)
        {
            printf("%d %d", arr[i][j]);
        }
    }
}